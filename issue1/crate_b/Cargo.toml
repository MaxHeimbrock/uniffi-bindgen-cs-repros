[package]
name = "crate_b"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
# The library that library-mode bindgen reads; it carries the UniFFI metadata of both crates.
crate-type = ["cdylib", "lib"]

[dependencies]
uniffi = { workspace = true }
crate_a = { path = "../crate_a" }
